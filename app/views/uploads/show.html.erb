<li>Description<p>
<%= @upload.description %></p></li>
<%= image_tag @upload.image.url(:medium) %>

<%= link_to 'Delete', @upload, method: :delete, data: { confirm: 'Are you sure?' } if current_user %>
<%= link_to 'Edit your pic', edit_upload_path %>
